# common containers

Contains mainly a docker-compose that i can use for common things while
building different applications. For example, I often need a postgres db so
instead of adding that in every service for local dev, I just come to this
folder and run docker compose up for it.
